class Solution:
    def runningSum(self, nums: List[int]) -> List[int]:
        lst = [0]*len(nums)
        print(lst)
        lst[0] = nums[0]
        print(lst)
        for i in range(1, len(nums)):

            lst[i] = sum(nums[0:i+1])
        return lst
